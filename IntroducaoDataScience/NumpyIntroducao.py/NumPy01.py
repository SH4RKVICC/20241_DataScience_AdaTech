#Aprendendo Numpy;
import numpy as np

lista = [1, 2, 3, 4, 5, 6]
array = np.array(lista) # Nota: Um Array, diferente de uma lista, possui apenas um tipo de dado

# Monstrando contéudo;
print(lista)
print(array)

# Mostrando tipo;
print(type(lista))
print(type(array))